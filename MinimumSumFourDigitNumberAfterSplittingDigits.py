class Solution:
    def minimumSum(self, num: int) -> int:
        lista = list(num)
        menorSoma =
        for i in range(len(lista)):

objeto = Solution()
num = 2932
print(objeto.minimumSum(num))